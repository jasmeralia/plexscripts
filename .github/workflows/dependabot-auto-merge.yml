name: Dependabot Auto-Merge

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && github.event.pull_request.number || github.ref }}
  cancel-in-progress: false

jobs:
  auto-merge:
    runs-on: ubuntu-latest
    if: github.actor == 'dependabot[bot]'
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Enable auto-merge
        run: gh pr merge --auto --squash "${{ github.event.pull_request.html_url }}"
        env:
          # A PAT, not GITHUB_TOKEN: pushes made with the default token do not
          # trigger other workflows, so merges done with it silently skip the
          # tag-and-publish workflow and no image gets built.
          GH_TOKEN: ${{ secrets.DEPENDABOT_MERGE_TOKEN }}
